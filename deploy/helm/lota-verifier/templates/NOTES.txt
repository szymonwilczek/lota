{{ include "lota-verifier.fullname" . }} is installed.

Replicas: {{ .Values.replicaCount }} (stateless; shared state in Postgres)
Service:  {{ .Values.service.type }} on port {{ .Values.service.port }} (TLS attestation)

Before the pods become Ready, make sure:
  - the Postgres DSN Secret exists
    {{- if .Values.postgres.existingSecret }} ({{ .Values.postgres.existingSecret }}, key {{ .Values.postgres.dsnKey }}){{- else if .Values.postgres.dsn }} (created by this release){{- else }}: set postgres.existingSecret or postgres.dsn{{- end }}
  - the TLS Secret {{ .Values.tls.existingSecret }} holds tls.crt and tls.key

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "lota-verifier.fullname" . }}

Reach the verifier in-cluster:
  {{ include "lota-verifier.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}
