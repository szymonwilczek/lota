===========
Performance
===========

* `Performance evaluation <evaluation.rst>`_ -- baseline measurements of the
  LOTA hot paths.

Methodology, tooling, and the L2/L3 runbooks live in ``benchmarks/README.rst``;
regenerate the raw data with ``make bench`` or ``benchmarks/scripts/run_all.sh``.
