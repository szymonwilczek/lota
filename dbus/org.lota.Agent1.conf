<!DOCTYPE busconfig PUBLIC
  "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
  "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<!--
  LOTA Agent D-Bus system bus policy.

  Only root may own the bus name. Members of the 'lota' group
  may call methods and receive signals. All exposed methods are
  read-only status queries; token generation requires the Unix
  socket SDK.

  Install to: /etc/dbus-1/system.d/org.lota.Agent1.conf
-->
<busconfig>

  <!-- Only root may own the bus name -->
  <policy user="root">
    <allow own="org.lota.Agent1"/>
    <allow send_destination="org.lota.Agent1"/>
    <allow receive_sender="org.lota.Agent1"/>
  </policy>

  <!-- Members of 'lota' group may call methods and receive signals -->
  <policy group="lota">
    <allow send_destination="org.lota.Agent1"/>
    <allow receive_sender="org.lota.Agent1"/>
  </policy>

</busconfig>
