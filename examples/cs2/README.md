# cs2

Intentionally empty: Not yet implemented.

Lands the CS2 / Proton hook smoke note: Steam launch options, Proton
versions exercised against `scripts/lota-proton-hook`, the expected
`/var/lib/lota/wine_token/<wine_pid>.token` artifact, and the runtime
behaviour when attestation is disabled.
