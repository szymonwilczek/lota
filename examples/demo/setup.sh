#!/usr/bin/env bash
# SPDX-License-Identifier: MIT
#
# Intentionally empty: Not yet implemented
#
# Final shape is a paced operator script that stands up swtpm,
# the verifier, the agent, the demo server, the heartbeat producer,
# and trust_pong in order, with a press-ENTER gate between phases.

set -euo pipefail
echo "examples/demo/setup.sh: not implemented yet" >&2
exit 64
